# @webkurier/cli-terminal

CLI interface for AI-powered web search in the WebKurier ecosystem.

## Installation

```bash
# From monorepo root
pnpm install

# Build CLI package
pnpm --filter=@webkurier/cli-terminal build