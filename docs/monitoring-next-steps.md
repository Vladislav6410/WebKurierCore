Recommended next steps
	1.	Extend /api/health to probe additional domain hubs (Chain, Security, Drone).
	2.	Add telemetry metrics (fps, latency, queue depth) to heartbeat payloads.
	3.	Integrate persistent encrypted state (blocks/encrypted_block_*.json) as the next reliability layer.