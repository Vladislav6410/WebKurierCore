{
  "storage_tokens": {
    "dropbox": "DROPBOX_TOKEN_HERE",         // Token для Dropbox (потом зашифруем через Chain)
    "google_drive": "GOOGLE_DRIVE_TOKEN_HERE", // Token для Google Cloud/Drive
    "cloud_backup_enabled": true
  },
  "ai_tokens": {
    "openai": "OPENAI_API_KEY_HERE",         // API to models + council
    "claude": "CLAUDE_API_KEY_OPTIONAL"      // OPTIONAL если используем debate models
  },
  "security": {
    "2fa_required": true,
    "rate_limit_per_min": 30
  }
}