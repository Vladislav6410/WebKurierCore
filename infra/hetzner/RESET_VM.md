# 🔄 WebKurierCore — Полный сброс и повторное развёртывание (Hetzner / Linux)

Этот документ поможет полностью очистить систему от старых контейнеров и собрать WebKurierCore заново с нуля.

---

## ⚠️ Внимание

- Все контейнеры и образы Docker будут **удалены**.
- Все данные из `/opt/webkurier/engine/memory` и `/opt/webkurier/engine/logs` **останутся**, если ты их не удалишь вручную.

---

## 🧹 1. Остановка и очистка Docker

```bash
docker compose down
docker system prune -a -f
docker volume prune -f
docker network prune -f